import { PlacaAutoPipe } from './placa-auto.pipe';

describe('PlacaAutoPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacaAutoPipe();
    expect(pipe).toBeTruthy();
  });
});
